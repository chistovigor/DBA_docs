# Ansible Role: `mongodb_memory_report`

## 🎯 Назначение

Роль предназначена для диагностики потребления памяти на хосте с MongoDB.  
Она собирает отчёты на уровне ОС и внутри MongoDB, чтобы определить:

- сколько памяти реально занимает процесс `mongod`,  
- какие базы и коллекции используют наибольшую часть кэша WiredTiger,  
- есть ли тяжёлые или подозрительные запросы, которые могут приводить к избыточному расходу памяти.  

---

## 📦 Использование

Пример запуска:

ansible-playbook -i ~/git_repos/infrastructure/mongodb mongodb_memory_report.yml --limit some_server / group --extra-vars "mongo_user=db_admin_username mongo_password=***"
